# Python
__pycache__/
*.pyc
*.pyo

# Logs & daemon runtime state (regenerated by the pipeline)
logs/
*.log
daemon_state_*.json

# Orchestration temp files (written/consumed by add_teams.py)
_add_players.txt
_retry_players.txt

# OS cruft
.DS_Store
Thumbs.db

# NOTE: the following ARE committed on purpose — they are the deployable data
# the Streamlit app reads at runtime on Streamlit Cloud (which never runs the
# pipeline). The .duckdb is a build artifact, shipped for one-click deploy:
#   player_vault.duckdb, positions.json, teams.json, nba_csv_exports/, wnba_csv_exports/
