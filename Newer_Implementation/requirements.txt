# Runtime dependencies for the deployed Streamlit app (vault_app.py).
# Kept minimal so the Streamlit Cloud build stays fast — the ingestion/API
# tools live in requirements-pipeline.txt and aren't needed to serve the app.
streamlit==1.58.0
duckdb==1.5.4
plotly==6.8.0
pandas==2.3.2
